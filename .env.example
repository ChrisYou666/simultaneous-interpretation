# 开发时默认走 Vite 代理（/api、/ws → localhost:8100），一般无需配置下列两项。
# 若曾用 setx / 系统环境变量设置过 VITE_API_BASE_URL，请删除该用户变量，否则会直连 8100、易连不上。
# 必须浏览器直连 Java（不经 Vite 代理）时：VITE_API_DIRECT=true 且 VITE_API_BASE_URL=http://localhost:8100
# VITE_API_DIRECT=
# VITE_API_BASE_URL=
# 可选：与后端 app.api-key / API_KEY 一致时传入
# VITE_API_KEY=your_server_api_key
